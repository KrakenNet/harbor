# IR JSON Schema

Authoritative JSON Schema for the Harbor intermediate representation.

> TODO: embed the generated JSON Schema (and a rendered field-by-field table) once `harbor.ir` finalizes its Pydantic models. Until then, see [Concepts: IR and schema](../concepts/ir.md) for the conceptual overview.
